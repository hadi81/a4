/* Since different ducks quack in different ways. So, QuackBehavior Interface is created to 
encapsulate these behavior so that different ducks can have their own quacking behavior by 
implementing this interface */
package com.example.demo;

public interface QuackBehavior {
 public String quack();
}
